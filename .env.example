# Environment variables for MistOrgLicensingComparison

# Required: Mist API Token (comma-separated for multiple tokens)
MIST_API_TOKEN=your_api_token_here

# Optional: Mist Organization ID (auto-detected if not set)
# MIST_ORG_ID=

# Optional: Mist API Host (default: api.mist.com)
# MIST_HOST=api.mist.com

# Optional: Log level (default: INFO)
# LOG_LEVEL=INFO

# Optional: Flask port (default: 5000)
# PORT=5000
